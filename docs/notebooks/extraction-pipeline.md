# Extraction Pipeline

Explore entity extraction backends — structured (batched), legacy (text parsing), and GLiNER — plus quality modes and fallback behavior.

<div style="margin: 0 -0.8rem">
  <iframe src="/nano-graphrag/notebooks/html/05_extraction_pipeline.html"
    style="width:100%; height:600px; border:1px solid var(--md-default-fg-color--lightest); border-radius:4px;"
    loading="lazy"></iframe>
</div>

## Run Locally

```bash
dotenvx run -- uv run quarto render notebooks/05_extraction_pipeline.qmd
```
